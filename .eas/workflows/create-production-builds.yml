name: Create Production Builds

on:
  push:
    branches:
      - main

jobs:
  build-ios:
    name: Build iOS (Production)
    type: build
    params:
      platform: ios
      profile: production

  build-android:
    name: Build Android (Production)
    type: build
    params:
      platform: android
      profile: production
